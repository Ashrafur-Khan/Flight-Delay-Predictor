# Base URL for the Python prediction API
VITE_API_BASE_URL=http://localhost:8000

# Optional: force the trimmed release UI in local development.
# Production builds default to the release UI unless this is set to false.
# VITE_RELEASE_UI=true
